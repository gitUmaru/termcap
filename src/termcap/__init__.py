"""termcap — record your terminal and export to text, SVG, GIF, PNG, JPEG, or MP4.

Self-contained core: PTY recording, an asciicast v2 reader/writer, and an ANSI
terminal emulator are all implemented here. GIF/PNG/JPEG are rendered with
Pillow; MP4 uses ffmpeg at runtime.
"""

__version__ = "0.1.0"
